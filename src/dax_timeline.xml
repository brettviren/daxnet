<class
    name = "dax_timeline"
    signature = "0"
    title = "DAX Timeline Message Codec"
    script = "zproto_codec_c"
    package_dir = "../include"
    >
  The DAX Timeline Codec.

  <define name = "BOT"  value = "0x8000000000000001"> 
    The end of time as measured in a 64 bit clock.
  </define>

  <define name = "EOT"  value = "0x7FFFFFFFFFFFFFFF">
    The end of time as measured in a 64 bit clock.
  </define>

  <message name = "STATUS">
    Request current valid epochs.
    <field name = "dtbeg" type = "number" size = "8">
      Request timeline valid after this given time.  If set to EOT,
      server uses its understanding of "now".
    </field>
  </message>

  <message name = "TIMELINE">
    A timeline.
    <field name = "ident" type = "number" size = "4">
      The highest ident of the epochs considered for this timeline.
    </field>

    <field name = "epochs" type = "msg">
      An ordered list of epochs as a message.  One frame per DEBUT.
    </field>

  </message>

</class>
